<Window
    x:Class="_02_StylesAndTemplates.CalendarsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:_02_StylesAndTemplates"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Calendars"
    Width="450"
    Height="250"
    ResizeMode="CanMinimize"
    mc:Ignorable="d">
    <Window.Resources>
        <Style x:Key="CalendarEmphasized" TargetType="Calendar">
            <Setter Property="Background">
                <Setter.Value>
                    <ImageBrush
                        ImageSource="Images/scribble.jpg"
                        Stretch="Fill"
                        TileMode="None" />
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Calendar
            Margin="10"
            HorizontalAlignment="Left"
            VerticalAlignment="Center" />
        <Calendar
            Margin="10"
            HorizontalAlignment="Right"
            VerticalAlignment="Center"
            Style="{StaticResource CalendarEmphasized}" />
    </Grid>
</Window>
